version: "3"

services:
    app:
        build: .
        env_file:
            - ./.env

    # db:
    #     image: postgres
    #     restart: always
    #     environment:
    #         POSTGRES_USER: $DB_USER
    #         POSTGRES_PASSWORD: $DB_PASSWORD

    # adminer:
    #     image: adminer
    #     restart: always
    #     ports:
    #         - 8080:8080
